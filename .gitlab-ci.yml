stages:
  - build
  - test
  - deploy

default:
  tags:
    - docker

variables:
  DATABASE_URL: 'mongodb://mongo/user_posts'

before_script:
- sudo gem install bundler
- cd reddit
- bundle install

build_job:
  stage: build
  script:
    - echo 'Building'

test_unit_job:
  stage: test
  script:
    - echo 'Testing 1'

test_integration_job:
  stage: test
  script:
    - echo 'Testing 2'

deploy_job:
  stage: deploy
  script:
    - echo 'Deploy'
